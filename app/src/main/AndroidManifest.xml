<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PocketLoa"
        tools:targetApi="31">
        <!--  bracelet options select Activity  -->
        <activity
            android:name=".view.auction.BraceletSelectActivity"
            android:exported="false"
            android:theme="@style/Theme.PocketLoa.NoActionBar"/>
        <!-- Auction Select Activity -->
        <activity
            android:name=".view.auction.EquipmentSelectActivity"
            android:exported="false"
            android:theme="@style/Theme.PocketLoa.NoActionBar" />
        <!-- Auction Search Result RV Activity -->
        <activity
            android:name=".view.auction.EquipmentRVActivity"
            android:exported="true"></activity> <!-- Intro splash -->
        <activity
            android:name=".view.intro.IntroActivity"
            android:exported="true"
            android:theme="@style/Theme.PocketLoa.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Main Activity -->
        <activity
            android:name=".view.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PocketLoa.NoActionBar"></activity>
    </application>

</manifest>